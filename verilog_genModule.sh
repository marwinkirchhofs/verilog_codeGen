#!/usr/bin/env sh

# this script handles the update queries for verilog_genModule.py
#
# a new commit is indicated as a "new beta update"
# a new tag is indicated as a "new version"

# change to repo directory (where this file resides)
cd $(dirname $0)

# TODO: do the git stuff
